apiVersion: apps/v1
kind: Deployment
metadata:
  name: fiap-produto-api-deployment
  namespace: fiap-produto-api
  labels:
    app: fiap-produto-api
spec:
  template:
    metadata:
      name: fiap-produto-api-deployment
      labels:
        app: fiap-produto-api-deployment
    spec:
      containers:
        - name: fiap-produto-api-deployment-container
          image: jacksoncarbonera/fiap-produto-api
          ports:
            - containerPort: 3000
          envFrom:
          - configMapRef:
              name: fiap-produto-api-config 
          - secretRef:
             name: fiap-produto-api-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            periodSeconds: 10
            failureThreshold: 3
            initialDelaySeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            periodSeconds: 10
            failureThreshold: 5
            initialDelaySeconds: 3
          resources:
            limits:
              cpu: "100m"
              memory: "100Mi"
            requests:
              cpu: "50m"
              memory: "50Mi"
  replicas: 1
  selector:
    matchLabels:
      app: fiap-produto-api-deployment