apiVersion: v1
kind: Service
metadata:
  name: fiap-produto-api-service
  namespace: fiap-produto-api

spec:
  type: LoadBalancer
  selector:
    app: fiap-produto-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000