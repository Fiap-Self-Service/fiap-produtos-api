version: '3.7'

services:

  fiap-produto-api:
    build:
      context: .
      dockerfile: Dockerfile
    networks: 
      - fiap-produto-api-net
    restart: always
    depends_on:
      - fiap-produto-api-db-mysql
    ports:
      - 3000:3000
    environment:
      DATABASE_HOST: fiap-produto-api-db-mysql
      DATABASE_PORT: 3306
      DATABASE_USERNAME: fiap
      DATABASE_PASSWORD: fiap
      DATABASE_DATABASE: fiap-produto

  fiap-produto-api-db-mysql:
    image: mysql:8.0
    restart: always
    networks: 
      - fiap-produto-api-net
    volumes:
      - mysql_data_fiap_produto:/var/lib/mysql
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: fiap-produto
      MYSQL_USER: fiap
      MYSQL_PASSWORD: fiap

volumes:
  mysql_data_fiap_produto: {}

networks:
  fiap-produto-api-net:
    driver: bridge